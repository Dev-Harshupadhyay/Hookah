# Canonical URL used for metadata, sitemap and robots
NEXT_PUBLIC_SITE_URL=https://hookah-baithak.vercel.app

# Optional: self-host the MediaPipe runtime + models instead of the CDN
# (run `npm run vendor:models` first, then uncomment)
# NEXT_PUBLIC_MP_WASM=/mediapipe/wasm
# NEXT_PUBLIC_HAND_MODEL=/models/hand_landmarker.task
# NEXT_PUBLIC_FACE_MODEL=/models/blaze_face_short_range.tflite
